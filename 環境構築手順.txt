docker-compose build
docker-compose up -d
docker exec -ti web /bin/bash
    dockerログイン後
    mysql -h db -P 3306 -u root -p

laravel appルートで下記を実行
composer install
cp .env.example .env
php artisan key:generate
.envは良しなに